// UI pagination defaults for the shop listing.

export const DEFAULT_PAGE_SIZE = 12;

// Matches the options exposed by the "Na stránku" native <select>.
export const PAGE_SIZE_OPTIONS: readonly number[] = [12, 24, 60];
